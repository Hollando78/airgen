# Backend service development defaults (loaded when API_ENV=development)
API_HOST=127.0.0.1
API_PORT=8787
API_JWT_SECRET=change_me
API_ENV=development
WORKSPACE_ROOT=./workspace

# Neo4j connection for local graph DB
GRAPH_URL=bolt://localhost:7687
GRAPH_USERNAME=neo4j
GRAPH_PASSWORD=airgen-graph
GRAPH_DATABASE=neo4j
GRAPH_ENCRYPTED=false

# Optional LLM provider credentials
LLM_PROVIDER=openai
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_BASE_URL=
LLM_TEMPERATURE=0.2

# Email + feature toggles
EMAIL_SYSTEM_BCC=
# ENABLE_ADMIN_ROUTES defaults to true outside production but can be overridden for local testing
ENABLE_ADMIN_ROUTES=
